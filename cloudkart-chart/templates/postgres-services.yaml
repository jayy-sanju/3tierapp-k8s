apiVersion: v1
kind: Service

metadata:
  name: postgres-service

spec:
  type: ClusterIP

  selector:
    app: {{ .Values.postgres.name }}

  ports:
    - protocol: TCP
      port: 5432
      targetPort: {{ .Values.postgres.containerPort }}